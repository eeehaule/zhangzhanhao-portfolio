# 内容信号档案 - 版式规则

## 页面目标

面向内容/AI 产品招聘方，用一条“内容证据带”让读者在 90 秒内理解候选人的角色、成果、方法与代表案例。

## 基础 Token

- 背景：`#F5F1E9`（纸张感），深色章节：`#173028`。
- 信号色：荧光黄 `#D8F65A`、蓝 `#4E68FF`、珊瑚红 `#FF725D`、薄荷绿 `#A9E6D0`。
- 显示字体：Noto Sans SC 700/800；情绪强调：Instrument Serif；数据与标签：DM Mono。
- 间距以 8px 为基础，正文最大行宽 34 个汉字，标题最大两到三行。

## 版式规则

- 每个章节只回答一个问题：做什么、如何工作、核心案例、设计判断或额外证明。
- 首屏使用“标题 + 主视觉 + 一个行动入口”，不堆数字、不放卡片网格。
- 成果数字只在“内容增长”章节出现一次；数字本身不作为装饰重复。
- 每页最多一个主视觉动作：拼贴图、证据条、流程线、原型窗口或文字主张。
- 所有案例都采用“问题 → 可见界面/产物 → 判断”的叙事顺序。
- 使用高对比色只标出状态、重点或章节切换，不将颜色作为唯一信息载体。

## 可访问性

- 所有链接和按钮具有文字含义与可见焦点。
- 提供关闭动态效果的开关；尊重系统“减少动态效果”偏好。
- 移动端单列，避免缩小桌面栅格。
